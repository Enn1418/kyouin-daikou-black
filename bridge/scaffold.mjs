/**
 * 教材フォルダの雛形づくり。
 *
 * Windows で担任が手作業でこれを用意しようとすると、確実に2つの罠を踏む:
 *   1. 拡張子が既定で隠れているため「学級の実態.md」が実は .md.txt になる
 *   2. メモ帳の文字コードが UTF-8 でないと、日本語が文字化けして読めない
 * どちらもブリッジ側で作れば起きない。
 *
 * 既にあるファイルは絶対に上書きしない。空でないフォルダには手を出さない。
 */
import { promises as fs } from 'node:fs';
import path from 'node:path';

/** 作るフォルダ。教材はここに溜まっていく。 */
const DIRS = [
  '00_共通',
  '00_共通/書式',
  '01_教材/国語',
  '01_教材/算数',
  '01_教材/自立活動',
  '00_共通/学習指導要領',
  '02_個別',
  '03_印刷テンプレート',
  '04_案件',
  '99_記憶'
];

const CLASS_PROFILE = `# 学級の実態

<!-- 未記入 -->

児童は匿名のID（A児・B児…）で書きます。**氏名は書きません。**
氏名との対応は、このフォルダの外（紙か別の場所）で管理してください。

エージェントは毎回このファイルを読みます。ここを書き換えるだけで、
出てくる教材の難易度と支援の入り方が変わります。
**書かれていないことは推測されません**（空欄のまま依頼すると、その欄について聞き返されます）。

分かる欄だけ埋めれば十分です。人数分だけ増やしてください。

## A児
- 学年:
- 国語:
- 算数:
- 集中の続く時間:
- 有効な支援:

## B児
- 学年:
- 国語:
- 算数:
- 集中の続く時間:
- 有効な支援:

## C児
- 学年:
- 国語:
- 算数:
- 集中の続く時間:
- 有効な支援:

## 学級全体
- 1こまの時間:
- プリントの分量:
- 避けたい刺激:
- 使っている教科書・教材:

<!-- 書き終えたら、いちばん上の「未記入」の行を消してください。 -->
`;

const RUBRIC_BASIS = `# 評価の根拠

ルーブリックを作るときに、どの学習指導要領を根拠にするかをここに書きます。
エージェントは毎回このファイルを読みます。

## 既定

小学校学習指導要領の**当該学年**を根拠にします。

## 例外

担任が「特別支援学校（知的障害）の各教科の段階」に言及したときだけ、
それを適用したルーブリックを**別表として併記**します。当該学年のものは残します。

## 条文の置き場

\`00_共通/学習指導要領/\` に、教科・学年ごとに本文を貼り付けてください
（例: \`00_共通/学習指導要領/算数_第2学年.md\`）。
ここに無いものは mext.go.jp を検索して確かめ、それでも確かめられなければ
「該当箇所を確認できません」と返します。**記憶からは書きません。**
`;

const COURSE_OF_STUDY = `# 学習指導要領（置き場）

このフォルダに、必要な教科・学年の本文を貼り付けてください。
ファイル名は「教科_第○学年.md」の形にすると、エージェントが見つけやすくなります。

例: 算数_第2学年.md / 国語_第3学年.md

**空のままでも動きます**が、その場合は mext.go.jp の検索に頼ることになり、
該当箇所を確かめられなければ「要確認」と返します。
`;

const CASES_README = `# 案件フォルダ（置き場）

ここには、アプリで「案件」を作るたびに、案件ごとのフォルダが自動でできます。
フォルダ名は案件名がそのまま使われます（例: \`算数 わり算の導入\`）。

\`\`\`
04_案件/
  算数 わり算の導入/
    01_教材/     ← プリントなど
    02_板書/     ← 板書計画
    03_掲示物/   ← 絵カード・おたより等
\`\`\`

**この形にしておくと、教材が増えても「あの単元の資料はどこ」で迷いません。**
単発の依頼（案件を作らずに1枚だけ頼んだもの）は、これまでどおり
\`01_教材/算数\` のように教科名のフォルダに入ります。

案件フォルダは中身を消しても構いません（アプリ側の記録は別に残ります）。
名前を変えると、アプリ側の案件名とずれるので、変えないことをおすすめします。
`;

const MEMORY = `# この学級での約束

差し戻したときの指摘がここに溜まります。エージェントは毎回これに従います。
手で書き足したり消したりしても構いません。

`;

const JIRITSU = `# 自立活動の区分・項目

**このファイルは空のままにしないでください。**

自立活動の区分と項目は、告示の本文をここに貼り付けてください。
エージェントは「このファイルに書かれている記述だけ」を根拠に紐づけます。
空のままだと、区分名を推測で書かずに「要確認」と返します（そういう作りにしてあります）。

`;

const FILES = [
  ['00_共通/学級の実態.md', CLASS_PROFILE],
  ['00_共通/自立活動_区分項目.md', JIRITSU],
  ['00_共通/評価の根拠.md', RUBRIC_BASIS],
  ['00_共通/学習指導要領/README.md', COURSE_OF_STUDY],
  ['04_案件/README.md', CASES_README],
  ['99_記憶/memory.md', MEMORY]
];

/** フォルダが存在しないか、中身が空か。 */
async function isEmptyOrMissing(root) {
  try {
    const entries = await fs.readdir(root);
    return entries.filter((e) => !e.startsWith('.')).length === 0;
  } catch (e) {
    if (e.code === 'ENOENT') return true;
    throw e;
  }
}

/**
 * 雛形を用意する。
 * 既に使われているフォルダ（空でない）には何もしない — 担任の作ったものを勝手に混ぜないため。
 * 戻り値は作ったものの一覧（何もしなければ空）。
 */
export async function scaffold(root, { force = false } = {}) {
  if (!force && !(await isEmptyOrMissing(root))) return { created: [], skipped: true };

  const created = [];
  await fs.mkdir(root, { recursive: true });

  for (const dir of DIRS) {
    const target = path.join(root, dir);
    try {
      await fs.access(target);
    } catch {
      await fs.mkdir(target, { recursive: true });
      created.push(`${dir}/`);
    }
  }

  for (const [rel, content] of FILES) {
    const target = path.join(root, rel);
    try {
      await fs.access(target);   // あるものは触らない
    } catch {
      await fs.writeFile(target, content, 'utf8');   // 必ず UTF-8 で書く
      created.push(rel);
    }
  }

  return { created, skipped: false };
}
