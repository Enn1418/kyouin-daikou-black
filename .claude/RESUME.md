# 現在地 — 教員向け（特別支援学級）エージェント職員室

このファイルは SessionStart フックが読み上げます。**作業を進めたら、このファイルも更新してください。**

---

## 引き継ぎメモ（2026-08-27・最新。まずここを読む）

### いま止まっていること（最優先で共有すべき事実）

**Claude API が利用上限に達していて、エージェントは動かない。**
担任がコンソールで設定した月の上限に到達したもので、こちらの不具合ではない。

- 復帰は **日本時間 9月1日（火）09:00**（原文は 2026-09-01 00:00 UTC）
- すぐ再開したい場合は console.anthropic.com → Settings → Limits で上限を引き上げる
- **上限が明けたら、設定画面（鍵のボタン）で「保存」を押すこと。**
  待っても直らないエラーのときは部屋の呼び出しを止める作りにしたので、
  保存が「担任が手を打った」合図になり、そこで再開する
- 画像生成（Gemini）・依頼票の入力・教材フォルダの読み書きは、この間も動く

### 担任が次にやりたいこと

1. **教材づくりを最後まで通す。** 依頼 →（秘書室）→ 段取り → 承認 → 制作 → 最終出力。
   まだ一度も最終出力まで到達していない。上限が明けるか引き上げたら、まずこれを試す
2. **出力形式にスプレッドシート（CSV）を追加。** 形式は CSV で決定済み（担任が選択）。
   `.csv` は教材フォルダの拡張子ホワイトリストに既に入っているので、
   `OutputFormat` に 'csv' を足し、`OUTPUT_FORMAT_LABEL` と `jobTools` の enum を揃えるだけで届く見込み
3. **児童向けアプリづくり**（将来）。教材の内容を踏まえたものを作りたい、という構想段階

### このセッションで直したこと（2026-08-27）

実地で教材を作ろうとして詰まった箇所を、順に潰した。

- **秘書室が止まる**（最大の不具合）。原因は3つ重なっていた:
  ① 脳が道具を実行したらターンを終えていた。依頼票に書き込んだだけで止まり、
  道具の結果（「全部埋まったので確定を」）を誰も読まない。
  → `AgentBrain.think` が**言葉で締めるまで考え続ける**ように（上限4周）。窓は10→20件
  ② 依頼票を画面で書いても部屋に伝わらない → `roomManager.notifyLead` で秘書長を起こす
  ③ 確定前は依頼票が誰にも配られず、部下が「依頼内容が提供されていません」と答えていた
  → 作成中の依頼票も「未確定」と明記して全部門に配る
- **案件の管理**。呼び出し中の印、中止／再開／削除（削除は2段階確認）、「続きを促す」
- **依頼票**: 読点が打てない不具合（1文字ごとに配列化していた）、
  「やりたい活動のイメージ」欄の追加、**Markdown 雛形の書き出し・読み込み**
- **エラーの日本語化**（`src/core/llm/explainError.ts`）。UTC を日本時間に直す。
  待っても直らない種類は部屋の再試行を止める
- 予算上限の既定を $2 → $5（20時間分の単元が途中で止まっていたため）

### いまの状態（2026-08-27 実測）

- **本線に未マージのものは無い。** 作業用 clone も未コミットの変更なし
- 担任のアプリは**更新済み**、ブリッジ(5174)とアプリ(3000)は**起動中**
- 絵は全部そろっている（顔42・全身42・背景10）。追加の課金は不要
- `99_記憶` はまだ空。差し戻しの経路が一度も通っていない

### 注意（このセッションで学んだこと）

- **画面で直したはずが直らない時は、ブラウザの保存データを疑う。**
  「Manage Teams」で編集した部屋は丸ごと localStorage に保存され、
  組み込み定義より優先される。コードもデータも正しいのに画面だけ違う、という形で出る
  （`listSystems` で重ね書きにして解決済み）
- **CSS の `lg:`（1024px）は当てにならない。** ノートPCの拡大表示 200% では
  1920 の画面でも実寸 960px。秘書室のハブ配置がこれで効かなかった
- Bash のヒアドキュメント経由で TypeScript を書くと `\n` が実際の改行になって壊れる。
  文字列リテラルを含む編集は Edit ツールで行う

---

## 引き継ぎメモ（2026-08-25）

### 担任が次にやりたいこと（本人から明示）

1. **スキルを作る。** ふりかえりで出した候補から選んで作成する。候補と中身は
   Obsidian の `AI実験室/claudecode記録/2026-08-25 スキル化の候補（教員代行アプリから）.md` に5件まとめてある
   （優先1位＝「変更を更新.bat運用のアプリに届ける」、2位＝「課金のかかる生成を見本→承認→一括で回す」）。
   作るときは `skill-creator` を使い、作ったあと `~/.claude/SKILLS.md` の更新も行う。
2. **教員代行の動作を一緒に確認して直す。** 実際に触って、気になったところをその場で修正していく。

### いまの状態（2026-08-25 実測）

- **本線に未マージのものは無い。** 直近のPRはすべて `claude/teacher-focused-design-kv19k9` に入っている
- 担任のアプリ（`kyouin-app2`）は**更新済み**、ブリッジ(5174)とアプリ(3000)は**起動中**
- 絵は**全部そろっている**: 顔42・全身42（担任含む）・部屋の背景10・見本1。追加の課金は不要
- `99_記憶` は**まだ空**。差し戻しの経路が一度も使われていない（担任は「使いながら様子を見る」方針）

### このセッションで入れたもの

フロア図を段（受付→設計→制作→仕上げ→点検→発信）で並べる形に作り直し、担当図鑑を新設。
職員室を暗いバーチャル空間の見た目に変更。3Dのキャラの頭上に似顔絵を出す。
フォルダボタンが教材フォルダの欄まで案内するよう修正（設定画面のスクロール不具合も同時に修正）。
担任だけ姿と色を変更（2頭身・猫耳・しっぽ・めがね・赤ペン、色は #9E30A6）。

### 見つかっている不備（次のセッションで直せる）

担任の絵（`90_職員室/全身/user.png`）を実際に見て、**指示文の不備が2つ**確認できた。

1. **「2頭身」が効いていない。** 出てきた絵は4〜5頭身の立ち姿。
   `TEACHER_FORM`（`src/core/office/agentArt.ts`）の書き方を強める必要がある
2. **背景に他のマスコットが写り込んでいる。** 原因は同じ指示文の
   「まわりのマスコットより少しだけ大きい」という一文で、**比較対象を描いてしまった**。
   大きさは絵の中で表現させず、表示側のサイズ指定（図鑑58px／他46px）だけで扱うべき

直すのは指示文だけ。**描き直しは1人ぶん（約20円）で済む。**

### 作業のルール（事故が2回起きたので必ず守る）

- **`kyouin-app2` の中で作業しない。** `更新.bat` がフォルダごと削除して入れ直すため、置いたものは消える
- 作業は `C:\Users\まどか\Projects\kyouin-dev`（本線ブランチの clone）で行い、PR経由で届ける
- PRの宛先（base）は必ず `claude/teacher-focused-design-kv19k9`。GitHubの既定は `main` だが、
  **`main` には先生用の中身が入っていない**
- GitHubの「Checking for the ability to merge...」が固まったら、**まず再読み込みして裏で終わっていないか確認**
  （実際に1回、終わっていた）。担任からは「固まったらコマンドでマージしてよい」と許可を得ている
- マージ後は `更新.bat` → **窓を全部閉じて** `起動.bat`。ブリッジは起動中のプログラムなので再起動しないと反映されない
- **担任は複数セッションを並行して走らせている。** 作業前に必ず `git fetch` して、
  本線が進んでいないか確かめる（今回も別セッションの2コミットが入っていた）

### ふりかえり（Obsidian）

`AI実験室/claudecode記録/` に 2026-08-25 付けで3本ある。
「AIとアプリを作るときの指示の型」「スキル化の候補」「教員代行努ブラック 取扱いの要点」。
**同種の作業に入る前にここを見ると、同じ手戻りを避けられる。**

---

## 引き継ぎメモ（2026-08-24 セッション終了時点）

前のセッションが長くなったため、ここで引き継ぐ。**下の「次にやること」が全ての残タスク。**
抜け漏れが無いように、このセッション中に CEO から出た指摘・要望をすべてここに落としてある。

### いま何が動いているか

CEO・秘書室・ワークフロー制御層・専門部門・品質管理室（独立監査）という階層の**P0〜P4が実装済み**
（設計は `docs/system-redesign.md`、CEO 承認済み）。加えて、実地確認で見つかった不具合3件を修正済み。
CEO からは最後に「作業が渡りました」の報告があり、**秘書室→単元設計室への引き渡しが少なくとも1回、
実際の API キーで動作したところまでは確認できている。** 承認②（完成物の提示）まで通したかは未確認。

続けて「教材フォルダが煩雑になっている」という指摘を受け、**案件ごとのフォルダ分けを実装済み**
（下の「完了済み（2026-08-24・続々）」を参照）。CEO 向けの整理ガイドも artifact として渡してある
（URL はこのセッションのチャット内。次のセッションには残らないので、聞かれたら作り直すか、
`action: "list"` で探す）。

**重要: このあと、別セッション（CEO が新しく開いたもの）が同じブランチに直接 push していたのが分かった。**
`git fetch` → `git merge origin/claude/teacher-focused-design-kv19k9`（コンフリクトなし、ort戦略で自動解決）
で取り込み済み。向こうのセッションは①③（フロア図をフロー図に）と②（役割一覧＝「担当図鑑」）を
**すでに実装していた**。加えて、色覚多様性に配慮したエージェント配色・ポートレート生成・記憶ビューア・
音声入力・職員室の見た目の暗色化などを追加していた（詳細は `git log` の各コミットメッセージが詳しい）。
**今後、このブランチで作業するときは、まず `git fetch` して同じことが起きていないか確かめること。**
CEO が並行して複数セッションを走らせている可能性がある。

### 次にやること（優先順位はCEOに確認。番号は独立したタスク）

**①UI改善**（CEO 指摘・2026-08-24）
1. ~~チャット入力欄の縦幅~~ → **完了**（このセッションで対応。`ChatPanel.tsx` に自動で伸びる
   入力欄を実装。既定3行、内容に応じて最大200pxまで伸びる）
2. ~~画面下部の SCHEDULED / ON HOLD 等の文字が薄い~~ → **完了**（`KanbanPanel.tsx` の列見出しと
   件数バッジ、空欄の「Empty」表示のコントラストを上げた）
3. ~~フロア図をフロー図の形に~~ → **別セッションで完了済み**（`FloorView.tsx` が上から下へ流れる
   ステージ表示に書き換わっている。動作確認はまだ）

**②役割一覧ページ** → **別セッションで完了済み**（`CharacterBook.tsx`。ヘッダーの「図鑑」ボタン）。
動作確認はまだ。

**P5〜P8（全体再設計の続き。`docs/system-redesign.md` §10 に定義済み、実装は未着手）**
- P5: 図書館（4体）＋ Google ドライブ同期フォルダを既存の参照フォルダ機構で読む
- P6: 単元設計室を「研究所」として作り直す（既存の資料集め・板書担当は削除して図書館・板書室に統合）
      ＋ 特別支援サポーター（4体・新設）
- P7: 書庫（3体）＋ 個人情報の保存前検査（`bridge/pii.mjs` 新設）
- P8: 視覚教材室を3→5体に拡張／板書室に画像確認担当を追加／画像とSVGを合成する仕組み

**実地確認の続き**
- 承認①→「実行」→制作→品質管理→承認②→完了まで、1件の案件を最後まで通す
- 差し戻し（品質管理が不合格を出す場面）が実際にどう見えるかの確認
- 出力の質そのもの（3段階の教材が同じ題材のまま分かれているか等）はまだ未確認

**その他**
- PR #1 は draft のまま。本文（P0・P1 のままで古い）は undraft のタイミングで書き直す
- `docs/unit-flow-design.md` は `docs/system-redesign.md` に統合済み（第1〜2層として現存）。
  読むなら後者が正。前者は経緯の記録として残してある

### 読んでおくべきドキュメント

- `docs/system-redesign.md` — 全体再設計案（CEO 承認済み）。実装の一次情報源
- `docs/unit-flow-design.md` — 単元を背骨にする案（上記に統合済みだが経緯として有効）
- `docs/floor-plan.md` — フロア図・同時実行の経緯
- `docs/teacher-edition-design.md` / `docs/teacher-edition-workflow.md` / `docs/setup-windows.md`

### 技術的な注意点（次のセッションがすぐ詰まらないように）

- `npm test` は `bridge/**/*.test.mjs`（node:test）と `src/**/*.test.ts`
  （`node --experimental-strip-types`）の両方を走らせる。**TS 側は import に拡張子（`.ts`）が必須**
  （Node の生 ESM 解決は拡張子省略を許さない。Vite 側は省略可なので混在に注意）
- `coreStore` / `jobStore` など zustand の persist ミドルウェアに依存するコードは、
  ブラウザ専用グローバル（`localStorage` 等）と拡張子なし import が絡み合っていて、
  **Node 単体では深い統合テストが組みにくい**（費用対効果が低いと判断し、これまでは見送ってきた）。
  ストア依存のロジック（`jobTools.ts` の `create_job` 等）は、ロジックを純粋関数に切り出せる範囲は
  切り出してテストする方針（`planning.ts` / `validatePlan.ts` / `permissions.ts` が実例）
- 権限（誰が何をできるか）は `src/core/agent/permissions.ts` に集約してある。新しい部屋・役割を
  足すときは、まずここに載せる権限があるかを検討すること
- `src/core/agent/AgentBrain.ts` の `concludeProject()` は、部屋の内部タスクが全部終わると
  自動でリードに完了を促す。**新しい「統括・監査系の部屋」を作るときは、ここに分岐を足し忘れると
  `deliver_project` を誤って呼んで部屋が止まる不具合が再発する**（2026-08-24 に実際に発生）
- 教材フォルダ（ブリッジ）関連のツール・プロンプト規約は、統括・監査系の部屋
  （`sec-*` / `qa-*`）には渡さない設計にしてある（`isOrchestrationRoom`）。新しい統括系の部屋を
  足すときは、同じ接頭辞の命名にするか、`ORCHESTRATION_LEADS` / `isOrchestrationRoom` に追記する

---

## 何を作っているか

私用 Windows PC で、特別支援学級（全学年・全教科／重点は算数・国語・自立活動）の教員本人が
一人で使うための教材作成エージェント。児童の個人情報は入力しない前提（匿名ID A児・B児…で運用）。

- 設計: `docs/teacher-edition-design.md`
- 実際の使い方（分刻み）: `docs/teacher-edition-workflow.md`
- 動かし方（Windows・手順）: `docs/setup-windows.md`
- ブランチ: `claude/teacher-focused-design-kv19k9` / PR #1

## 完了済み（P0）

- `src/data/teacherAgents.ts` — 特支向け4チーム（同時異教材室・国語教材室・算数教材室・自立活動室）
  各チームはリード1＋サブ3。サブは `claude-haiku-4-5`、リードは既定モデル
- `src/core/agent/PromptBuilder.ts` — `teamType === '特別支援'` のときだけ効く規約
  （日本語・匿名ID・自立活動の区分は一次資料のみ・評価は「下書き」明記・教科書本文の複製禁止）
  あわせて特支チームのみ `complete_task` / `deliver_project` の100語制限を解除
- `src/data/agents.ts` — 既存定義を `BASE_SETS` に改名し `AGENTIC_SETS` に合流（既存チームは無変更）
- `npm run lint` / `npm run build` 通過

## 完了済み（P1：ローカルブリッジ）

- `bridge/server.mjs` — 教材フォルダのブリッジ。127.0.0.1 のみ、トークン認証、root 配下限定、
  拡張子ホワイトリスト、上書き時 `.bak`、削除 API なし。`/health` `/files` `/file` `/memory` `/export`
- `bridge/markdown.mjs` `bridge/templates.mjs` — 依存なしの Markdown→HTML と印刷テンプレート
  （plain / grid / trace / spaced / one-task、BIZ UDPゴシック指定）。
  教材フォルダ側に `03_印刷テンプレート/<名前>.css` があればそちらが優先
- `src/core/bridge/bridgeClient.ts` / `src/integration/store/bridgeStore.ts` — 接続設定と接続確認
- `src/core/agent/tools/fileTools.ts` — `list_files` / `read_file` / `write_file`
  （ブリッジ接続時のみツール定義に載る。read は全フェーズ、write は working のみ）
- `ToolRegistry.process` が文字列も返せるようになり、`AgentBrain` がそれを tool_result に渡す
- `BYOKModal` に「教材フォルダ（任意）」欄と接続確認ボタン
- 起動: `npm run bridge -- --root "D:\kyouin"` → 表示された URL とトークンをアプリの設定に貼る
- `bridge/scaffold.mjs` — フォルダが無い／空のときだけ雛形を作る（UTF-8 で書くので文字化けしない）。
  空でないフォルダには触らない。`--init` で足りないものだけ補完。既存ファイルは上書きしない

## 完了済み（ドリル生成）

- `bridge/drill.mjs` — 出題条件（型）から決定的に問題を作る。全列挙して絞り込むので、
  条件が厳しくても無限ループにならず、足りなければ足りないと言う。seed が同じなら同じプリント
- `POST /generate/drill` — 児童用プリントと教員用解答を**別ファイル**で書き出す。印刷用HTMLも同時に可。
  条件に合う問題が0問ならファイルを作らず断る
- エージェント用ツール `generate_drill`。プロンプト S10 で「計算問題を自分で並べない」を明示
- `bridge/drill.test.mjs` — `npm test`（node:test、依存なし）。答えの正しさ・条件遵守・
  重複なし・再現性・児童用に答えが載らないことを固定。11件

## 完了済み（長期記憶）

- `bridge/memory.mjs` + `POST /memory/note` — 差し戻しの指摘を1件足す。同じ指摘は増やさない
  （日本語では語間の空白に意味がないので、比較時のみ空白を無視）。上限100件で古いほうから落とす
- `coreStore.rejectTask` から `appendMemoryNote` を投げっぱなしで呼ぶ（失敗しても差し戻しは成立）
- `checkBridge` 成功時に `/memory` を読み、`PromptBuilder` が
  「この学級での約束」としてプロンプト先頭付近に載せる（末尾4000字まで）
- `bridge/memory.test.mjs` — 7件。重複排除・空入力・改行の畳み込み・上限・担任の手書き見出しの保全

## 完了済み（既定チームとアバター配置の修正）

- `DEFAULT_AGENTIC_SET_ID` を `'sn-multi-tier'`（特支・同時異教材室）に。
  以前は実在しない `'single-agent'` で、常に `AGENTIC_SETS[0]`（unboring-net）へ落ちていた
- `NpcAgentDriver` の円状配置の分母を `MAX_AGENTS`（固定5）から実際のアバター数
  （`characterManager.getCount()`）に変更。9エージェントのチームで index 6〜9 が 1〜4 に重なっていた

## 実地確認で分かったこと（2026-08-22・担任の Windows PC で初回起動）

起動・4体のアバター・チーム選択までは問題なし。詰まったのは全部この周辺だった。

- **ブリッジの接続状態を localStorage に残していなかった** ので、再読み込みのたびに
  未接続へ戻り、ファイル系ツールが黙って消えていた。→ 起動時に自動で繋ぎ直す（`autoConnectBridge`）
- **トークンを起動のたびに作り直していた** ので、ブリッジを再起動すると設定が無効になっていた。
  → 教材フォルダ直下の `.bridge-token` に保存して使い回す（ドット始まりなので一覧にも出ず、
  拡張子ホワイトリストにも当たらないのでブリッジ越しには読めない）
- **接続状態が画面のどこにも出ていなかった。** → ヘッダーにフォルダのアイコン（緑/灰/赤）
- **URL とトークンが横並びの無ラベル入力**で、トークン欄が見つけられなかった。
  → 縦積み＋①②の見出し。押せない理由もその場に書く
- **ブラウザの自動翻訳で画面が真っ白になった。** `lang="en"` のままだったので Chrome が翻訳を提案し、
  翻訳がテキストノードを差し替えた直後に React が落ちていた。
  → `lang="ja" translate="no"` ＋ notranslate、加えて `ErrorBoundary` で白画面を避ける
- プロンプト S8 を具体化（`00_共通/学級の実態.md` を最初に読む）。
  未接続時は「教材フォルダを接続すると実態を読める」と言い添えるようにした

**まだ確認できていないのは出力の質そのもの**（3段階の差・分量・保存先）。ここが次の焦点。

## 完了済み（図・2026-08-22）

- `bridge/figures.mjs` — 教材の図を決定的に描く。ドリルと同じ線引きで、
  「どの図をどの数で」はエージェント、「実際に描く」のはコード。白黒・太線の SVG。
  tenframe / dots / numberline / container（かさ）/ clock / tape / fraction / coins / grid
- Markdown の ```図 ブロック（`key: value` 行）を `markdown.mjs` が SVG に変換する。
  .md のままなら指示として読め、印刷用 HTML では図になる
- 指示の誤り（入れ物より多い水、分母超えの分子、実在しない硬貨、未知の種類）は
  例外にせず赤枠の注記にする。プリント全体が消えるより、その1か所が直せるほうがよい
- プロンプト S11 に種類と指示の一覧。「絵文字・記号で図を描かない」
- `bridge/figures.test.mjs` — 19件。個数の一致・比の正しさ・ありえない指示の拒否

## 実地確認2回目で分かったこと（2026-08-22）

- **`scaffold.mjs` が架空の児童データを雛形として書き出していた**（A児1年・B児3年・C児5年）。
  担任が書き換える前に教材へ流れ込む危険があり、実際に「入れた情報と違う」という報告になった。
  → 雛形は全欄空にし、先頭に `<!-- 未記入 -->` を置く。回帰テスト付き
- プロンプト S2 を強化：学年・到達度は**書かれている文字列だけ**を根拠にする。空欄は埋めない
- S8：実態を読んだ結果を最初の1行で報告させる（「実態: 読込 A児/B児/C児」「実態: 未記入」）。
  読めたかどうかが担任から見えないと、間違いに気づけない

## 実地確認3回目（2026-08-22）— 実態は読めていた。指示の側が間違っていた

担任の実際の `学級の実態.md` は **A児〜G児の7名（1〜4年）**。エージェントは確かに読んでいた
（A児1年・B児2年は記述どおり）。にもかかわらず3人分しか作らなかったのは、
**こちらが渡した依頼文が「A児・B児・C児の3段階で」だったから**。
架空の3人組の雛形をなぞった文面を手順書に載せていた。

- リードの手順を修正：人数は実態記述のとおり。3人と決めつけない。**全員をどれかの段階に割り振る**
  （1段階に複数人でよい）。担任が絞ったときだけその児童に限る。誰も取りこぼさない。
  各プリントの先頭に「対象: ○児・○児」を書く
- 学級全体の欄（集中15分・1枚に収める・使用教材）を時間配分と分量に反映させる
- プロンプト S12 を追加（全特支チーム共通）：人数を勝手に減らさない・対象児童を明記
- 手順書の依頼文例を「学級全員を3段階に分けて」に差し替え。児童IDを並べる必要はない
- サブエージェントの指示を図ブロック前提に更新（`[レイアウト]` は写真など描けないものだけ）

**教訓**: 雛形にも手順書にも架空の具体例を書くと、それが仕様として流れ込む。
例示は「形」だけにして、中身は担任のファイルから来るようにする。

## 完了済み（単元設計チーム・2026-08-22）

担任の回答4点を受けて実装した。計画と回答は `docs/unit-design-plan.md`。

- `sn-unit-design`「特支・単元設計室」— 単元デザイナー＋問いづくり／評価／資料集め／板書の5体。
  **ゴールはリードが決めて承認を取ってから**下流に発注する（5つの作業には依存関係があるので並列化できない）
- リードは**参加児童を担任に確認してから始める**。単元によって7名全員が参加するとは限らない
- 評価の根拠は**当該学年**が既定。担任が特別支援学校（知的障害）の各教科の段階に言及したときだけ併記
  （置き換えない）。プロンプト S13 と `00_共通/評価の根拠.md`
- `bridge/figures.mjs` に `board` 型。**黒板は 3600×1200mm（3:1）に固定**。
  小学校で最も一般的な寸法（幅2400〜3600・高さ900〜1200の範囲で最多）。可変にすると
  「入りきらない」の判断ができなくなる
- `ToolRegistry` が**評価担当と資料集め担当にだけ** Anthropic のサーバ側 web 検索を渡す
  （`WEB_SEARCH_AGENTS`）。評価は mext.go.jp 限定・最大5回、資料集めは制限なし・最大8回
- `ClaudeProvider` がサーバ側ツール（`type: 'server'`）を変換せず素通しする。GeminiProvider では落とす
- 資料集めは**授業づくりの資料だけ**。児童の学習状況の記録は集めない（担任の指示）
- テスト 51件

**未確認**: 単元設計チームはまだ一度も実際に動かしていない。
検索が長引くと `pause_turn` で止まる可能性があり、再開処理は入れていない。

## 完了済み（図を画像として取り出す・2026-08-22）

- `bridge/saveimage.mjs` — 印刷用 HTML に埋め込む「画像として保存」ボタン。
  SVG→PNG の変換は**担任のブラウザ（canvas + toBlob）が行う**。
  変換ソフトの導入を求めない／ブリッジに画像処理の依存を入れない、の両方が目的
- 倍率4倍。板書は **3600×1200px**（実物 3600×1200mm と 1px = 1mm）。背景は白で塗る。
  `@media print` でボタンは紙に出ない
- **実機確認済み**: Chromium（Playwright）で見本 HTML を開き、ボタンを押して
  225KB・3600×1200 の PNG が保存されることを確認した
- `bridge/templates.test.mjs` — 5件。テスト 56件

## 完了済み（板書の作り直し・2026-08-22）

担任から「板書の質が悪すぎる」。実際そのとおりで、旧 `board` は区画に見出しを置いた帯だった。
リサーチしてから作り直した（出典は `docs/teacher-edition-design.md` の板書計画の節）。

- **調べて分かった要件**: 1時間の流れが1枚に残る／めあて青枠・まとめ赤枠／赤い文字は見えにくい／
  **文字の大きさは学年で決まる（低12cm・中10cm・高8cm）**／特支はカード化と構造化
- `bridge/board.mjs` — 新しい板書。区画×部品（label / box / card / bubble / text / figure）。
  **区画の中に図を入れられる**。指示は入れ子になるので `図` ブロックの中身を JSON で書く
  （`parseFigureSpec` が `{` 始まりなら JSON として読む）
- **入りきるかを計算する**。学年の文字サイズから縦横の字数を出し、溢れた区画を赤枠で囲んで
  「◯mm 分 入りません」と書く。書ききれない板書計画を黙って出さないため
- `sn-board`「特支・板書室」— 板書デザイナー＋**リサーチ／配置設計／図解づくり**（担任の提案）。
  リードは**2段階で発注**する（リサーチ→配置＋図解）。並列にすると根拠のない配置になる
- `sn-board-research` に web 検索（最大6回）
- **直したバグ**: 入れ子にした図が印刷用CSSの `.figure { height: auto }` で高さ0に潰れていた。
  入れ子側の `class="figure"` を外す。回帰テスト付き
- 板書の PNG は 4800×1600（実物 3600×1200mm）。Chromium で保存まで確認済み
- テスト 64件

## 完了済み（参照フォルダ・2026-08-22）

担任「元になる資料が足りない。NotebookLM や Obsidian のものを読み込めないか」への回答と実装。

- **Obsidian は繋がる**。保管庫はただの Markdown フォルダなので、読めるようにするだけでよかった
- **NotebookLM は一般向けの公開 API が無い**（企業向けの Gemini Notebook Enterprise のみ。
  非公式ライブラリは未公開エンドポイント依存なので採用しない）。
  代わりに「元資料を参照フォルダに置く」「Google ドキュメントに書き出し→ドライブ同期→参照フォルダ」
- `bridge/roots.mjs` — `--ref "名前=パス"` で参照フォルダを何個でも足せる。**読み取り専用**。
  名前がぶつかったら番号を足す（黙って別フォルダを読ませない）。パス脱出は従来どおり遮断
- `bridge/search.mjs` + `GET /search` — 保管庫は数千ファイルになるので一覧は成り立たない。
  語で探して当たった前後だけ返す。複数語は AND。隠しフォルダは見ない。512KB/4000ファイルで打ち切り
- ツール `search_files`、`list_files` / `read_file` に `root`。**参照フォルダがあるときだけ**載る
- プロンプト S8-0: 単元を決める前に担任のノートを探す（担任の記録＞こちらの一般論）
- **直したバグ**: PUT /file が `root` を無視していた。「ノートに書いて」が黙って教材フォルダに
  書かれ、成功と返っていた。いまは参照フォルダへの書き込みを 403 で断る
- 読めるのは `.md .txt .csv .json .html` のみ。**PDF・Word は読めない**（依存を入れない方針）
- テスト 75件

## 完了済み（更新の手間をなくす・2026-08-22）

担任「最初の ZIP 以降を入れていない。どこから始めればいいか」。
ZIP は常に累積なので入れ替え1回で足りるが、毎回 PowerShell を貼るのは負担なので用意した。

- `scripts/更新.bat` — 最新の ZIP を落として `kyouin-app2` を作り直し、`npm install` まで
- `scripts/起動.bat` — ブリッジとアプリを2窓で起動。`REFPATH` を書けば `--ref` が付く
- **担任には「1つ上のフォルダにコピーして使う」と伝える**（更新でアプリ側は消えるため）
- `.bat` は CRLF でないと誤動作するので `.gitattributes` で固定
- `%REF%` のような未定義変数はバッチではそのまま文字列として渡ってしまうため、
  `if defined` で分岐する形にした

## 旧・進行中（2026-08-22）— 単元設計チーム

計画は `docs/unit-design-plan.md`。**担任への確認4点の回答待ちで、実装は未着手。**
要点: ゴール決定はリードが担い承認ゲートを1回置く（5つの作業に依存関係があるため並列化できない）。
学習指導要領は記憶から書かせず、担任の一次資料 → mext.go.jp 限定の web_search の二段構え。
特支の読み替え（当該学年／下学年適用／特別支援学校の各教科の段階）は推測不能なので担任に聞く。
板書は `figures.mjs` に `board` 型を足して図として描く。

- 職員室の背景色を7色から選べるようにした（`appearanceStore` + ヘッダーのパレット）。
  3D の背景のみ。UI 配色は変えない

## 完了済み（2026-08-23）— フロア図・同時実行・広報室

担任「各部屋が一つの空間に入り、それぞれが作業している様子が分かるようにしたい。
毎回切り替えるのが煩わしい。今後も部屋を増やす。進捗とどこで何が行われているかが大切」。
担任の決定4点: ①間取り図のまま家具に色 ②同時実行してよい ③画像生成は掲示物・おたより・
インフォグラフィック ④役割を細かく区切って新キャラクターを配置。経緯は `docs/floor-plan.md`。

- **フロア図**: `src/interface/FloorView.tsx` — 全部屋を1画面に俯瞰する既定画面（`viewMode: 'floor'`）。
  `AGENTIC_SETS` から部屋が自動で並ぶ。机＝担当、様子・進捗・**承認待ち件数**を部屋の外から見せる。
  床は部屋色で薄く塗り、備品（ホワイトボード・本棚・観葉植物）を記号で置き、机の縁に担当色
- **同時実行（本命・実装済み）**: `coreStore` を `rooms: Record<RoomId, RoomState>` に全面改修。
  担当の状態は `${roomId}:${index}` で引く。roomId を AgentBrain / PromptBuilder / ツール群 /
  AgentHost / AgentSimulation に通した。**`src/core/rooms/RoomManager.ts`** が部屋ごとの
  AgentSimulation を持ち、部屋を切り替えても dispose しない（3D は覗く窓）。
  **旧: チーム切替で `resetProject()` を呼ぶ購読が仕事を消していた**。これを削除
- **特支・広報室（`sn-koho`）**: 掲示物・おたより・インフォグラフィック。広報ディレクター＋
  文面担当＋ビジュアル設計＋校正・安全確認。`outputType: 'image'`（Gemini、鍵は既存の欄）。
  原則: 文字は Markdown→印刷、画像は文字なしの挿絵に徹する（画像モデルは日本語を崩す）。
  おたよりは匿名IDすら書かず集団の言い方。**板書・教材の図には画像生成を使わない**
- `MAX_AGENTS` を 5→10（役割を細かく区切る前提）
- lint / build / test 75件 / Chromium で7部屋の表示確認 済

**未確認**: 実機（担任のPC）で2部屋を同時に走らせたときの挙動と、広報室の実出力。

## 完了済み（2026-08-23・続）— 特支・視覚教材室

担任「既存のNano Banana Labを再利用する形で視覚的な教材作成専門の部署をつくってほしい」。

- `sn-visual`「特支・視覚教材室」（`src/data/teacherAgents.ts`、8部屋目）。既存の `photo-studio`
  （Nano Banana Lab）の型＝「主題＋動作＋文脈＋構図＋様式」の画像生成プロンプト作りをそのまま踏襲
- 視覚教材ディレクター（承認はあなた）＋ 主題・場面担当 ＋ 構図・様式担当 の3人構成
  （Nano Banana Lab と同じ人数構成に合わせた）
- 作るのは絵カード・視覚支援ツール（スケジュール表や手順書の挿絵）・ソーシャルストーリーの挿絵・教材の挿絵
- **数が合う図（十マス・時計等）と板書はこの部屋の仕事ではない**と明記（figures.mjs / board.mjs に譲る）
- シリーズ内で画風・色数を揃える、画像に文字を入れない、実在人物・キャラクターは描かせない、を担当に明記
- 個人情報（匿名IDすら不要な人物像の多様化）は `teamType: '特別支援'` の共通規約でそのまま効く
- 色 `#FACC15`（Nano Banana=黄系）。lint / test 75件 / Chromium で8部屋表示確認 済

## 完了済み（2026-08-24）— 全体再設計 P0〜P4

CEO（利用者）の再設計要求。設計は `docs/system-redesign.md`（承認済み）。
階層: CEO → 秘書室 → ワークフロー制御層 → 専門部門 → 品質管理室（独立監査）。

- **P0** 表示名を CEO に。プロンプトは S0 で「CEO（担任）」と一度だけ定義し、
  60か所以上ある役割説明は書き換えていない
- **P1** `src/core/jobs/` — 案件（Job）と依頼票（RequirementSheet）。必須10項目。
  **充足判定は機械的**（LLMに確認させると見落として制作が始まる）。
  `SHEET_FIELDS` 1つが画面・検証・質問文を駆動する。要約を S14 として全部門に同一文面で注入。
  `JobBar`（ヘッダー）＋ `JobSheetModal`。案件を離れれば従来どおり部屋に直接頼める
- **P2** `src/core/workflow/` — 制御層。判断は `planning.ts`（純粋関数）、実行は `WorkflowEngine.ts`。
  承認①前は1工程も起動しない／同じ部屋を二重起動しない／提出は必ず検査へ／
  差し戻し3回でCEOへ／上限額で停止／依存先が失敗したら待たずに上げる。
  **直したバグ**: 差し戻しを「部屋を占有」扱いにして、起動も解放もできない状態を作っていた
- **P3** 秘書室5体（`secretariatAgents.ts`）＋ `set_work_plan`（段取りの**検査つき**登録：
  実在しない部門・ID重複・存在しない依存・循環・完了条件なし・依頼成果物の欠落を弾く）
  ＋ 承認①の2枚（`documents.ts`。**決定的に組み立てる**。未確定は隠さず「未確定」と出す）
- **P4** 品質管理室4体（`qaAgents.ts`）＋ `submit_qa_verdict`。
  **室長は合否を出すだけで完了にできない**（進路を決めるのは制御層＝独立性の実体）。
  別部屋なので制作部門と履歴を共有しない。engine が成果物と完了条件だけを渡す

テスト: bridge 75件 ＋ TypeScript 66件（`npm test` が両方走る。
TS は `node --experimental-strip-types`。import は拡張子つきで書くこと）。

**未確認**: 実際のAPIキーで依頼→承認→制作→検査を1本通していない。ここが次の焦点。

## 完了済み（2026-08-24・続）— 秘書室が案件を作れなかった不具合

CEO の実地確認: 秘書室に依頼を伝えても、案件を作る手段がなく行き止まりだった（③）。
続けて発生した2つ目の不具合: 案件は作れても、会話が「実態確認します」で止まって見えた。

- **1つ目の直し**: `create_job` / `update_requirement_sheet` / `lock_requirement_sheet`
  （`src/core/agent/tools/jobTools.ts`）。秘書室の窓口（`sec-chief`・`sec-intake`）だけが使える。
  idle でも working でも渡す（CEO は最初のひとことで依頼するため）。
  推測で埋めない規約はツールの説明文に書く（モデルが実際に読む場所）
- **2つ目の直し（本質的な原因）**: 部屋の心拍（`AgentSimulation` の定期実行）は
  `phase === 'working'` のときしか回らない。`create_job` は部屋を idle のまま案件だけ作っていたので、
  最初の受け答えのあと誰も次の一手を打たず、会話が止まって見えていた。
  `create_job` の時点で `startProject()` を呼んで部屋を働かせるよう修正（idle のときだけ。
  すでに working なら上書きしない）
- 権限（誰が何をできるか）を `src/core/agent/permissions.ts` に集約し、直接テストできるようにした
  （以前は各ツールファイルの中にあり、ストア依存のためテストから届かなかった）

**続報（2026-08-24）**: 上記だけでは足りず、CEO の実地確認で「秘書室は進むが、その先（単元設計室）が
動き出さない」ことが判明。根本原因はエンジン側の隠れた挙動だった。

- `AgentSimulation.checkProjectCompletion()` → `AgentBrain.concludeProject()` が、
  **部屋の内部タスクが全部終わると、リードに「deliver_project を使え」と固定文面で促す仕組み**
  （通常の制作部屋を前提にしたもの）
- 秘書室・品質管理室のリードにも `deliver_project` を渡していたため、リードはその指示に従い、
  本来呼ぶべき `set_work_plan`／`submit_qa_verdict` ではなく `deliver_project` を呼んでしまい、
  **その部屋の phase が 'done' になって心拍が止まった**（心拍は working のときしか回らない）。
  単元設計室への段取り登録が一度も起きなかったのは、これが直接の原因
- 直し: `permissions.ts` に `ORCHESTRATION_LEADS`（sec-chief・qa-chief）を追加。
  ①`ToolRegistry` がこの2人には `deliver_project` を渡さない
  ②`AgentBrain.concludeProject()` が、この2人には部屋に合った文面（段取りの状態を確認する／
  検査結果をまとめて判定する）を送るよう分岐
- 併せて `set_work_plan` に、承認待ち①以降の案件へ段取りを重ねて登録できないようにする
  安全弁を追加（二重呼び出しで承認済み・進行中の工程が黙って消える事故を防ぐ）
- 回帰テスト: `permissions.test.ts` に4件追加（合計83件）

**続報2（2026-08-24）**: CEO が秘書長にチャットで返答したところ、
「ERROR: ブリッジに接続できません…」が秘書長の発言として表示された。原因は2つ重なっていた。

1. **見た目の問題**: `AgentBrain.think()` はツール実行後、同じターン内でモデルを呼び直さない。
   ツールの実行結果（`role: 'tool'`）を internal 扱いにしていなかったため、
   `ChatPanel` が tool_result の生の文字列（ここではブリッジの接続エラー）を、
   そのままエージェント本人の発言として表示していた
   → **直し**: `AgentBrain.ts` の tool_result 履歴に `metadata: { internal: true }` を付与
2. **秘書室がそもそも読みに行く必要がなかった**: `teamType: '特別支援'` の部屋には
   「依頼を受けたらまず学級の実態ファイルを read_file で読む」という規約（S8）が一律に効いていた。
   秘書室・品質管理室は教材フォルダを扱う仕事ではないのに、この規約のせいで
   秘書長がブリッジ未接続のまま読み込みを試み、エラーになっていた
   → **直し**: `permissions.ts` に `isOrchestrationRoom`（`sec-`／`qa-` 接頭辞）を追加。
   `ToolRegistry` がこの2部屋には教材フォルダ関連のツール自体を渡さないようにし、
   `PromptBuilder` も S8 の代わりに「教材フォルダはこの部屋の仕事ではない」という一文に差し替え

回帰テスト: `permissions.test.ts` に3件追加（合計86件）。

**未確認**: 実際のAPIキーでの動作は、担任のPCでの再試行待ち。
Node単体では coreStore/jobStore がブラウザ依存の import グラフ（拡張子なしの相対import、
zustand persist の localStorage 等）を持つため、深い統合テストは費用対効果が低いと判断し見送った。

## 完了済み（2026-08-24・続々）— 教材フォルダが煩雑になる問題

CEO「フォルダが煩雑になってしまっていることも気になります。最初のフォルダ作成のコツもまとめてください」。

原因は、置き場所が「部屋ごと・教科ごと」にしか決まっておらず、**単元・案件の単位でまとまっていなかった**こと。
`01_教材/算数` に、違う単元の教材が全部フラットに積まれていく作りだった。

- `src/core/jobs/folderName.ts`（新規） — 案件名から Windows で安全なフォルダ名を作る
  （`\ / : * ? " < > |` を置換、末尾のピリオド・空白を除去、長すぎる名前は切り詰め）。
  `caseFolderPath(title)` が `04_案件/<案件名>` を返す
- **S14（依頼票の要約。全部門に同一文面で注入）に「保存先」を追加**。
  案件が確定していれば `保存先: 04_案件/<案件名>/ の下（.../01_教材/、.../02_板書/、.../03_掲示物/）`
  という1行が自動で全部門に伝わる。**部屋ごとの個別対応は不要**（S14を経由するので、
  板書室・視覚教材室・広報室も含め、教材フォルダを使う部屋すべてに自動で効く）
- `PromptBuilder.ts` の S9 を更新: 保存先の指定があれば必ずそこへ、単発の依頼のときだけ
  従来どおり教科フォルダへ
- `bridge/scaffold.mjs` — `04_案件/` フォルダと、置き場所の考え方を説明する README を雛形に追加。
  **`--init` で既存の（空でない）教材フォルダにも後付けできる**（既存ファイルは一切上書きしない）
- CEO 向けの整理ガイド（artifact・HTML）を作成: なぜ煩雑になったか／フォルダ構成の図解／
  案件を作る判断基準／最初のフォルダ作成のコツ6点／すでに散らかった分の扱い方
- テスト: `folderName.test.ts` 7件、`scaffold.test.mjs` に2件追加、`requirementSheet.test.ts` に2件追加
  （合計 bridge 77件・TypeScript 95件）

**未確認**: 実際に案件を作って、教材フォルダに `04_案件/<案件名>/01_教材/...` の形で
保存されるところまでは、担任のPCでの再試行待ち。

## 次にやること（旧・古い一覧。最新は冒頭の「引き継ぎメモ」を見ること）

このセクションは全体再設計（P0〜P4）着手前の古いメモ。内容は冒頭の「引き継ぎメモ」に
統合済みなので、以後はそちらだけを更新すればよい（ここは経緯としてそのまま残す）。

1. 出力の質を見る — 「かさ（LとdL）」等で1回通し、3段階が同じ題材のまま分かれているか・
   45分1枚に収まるか・`01_教材` に保存されるかを見る
2. UI で記憶を見る／直す — 現状 memory.md は担任がエクスプローラで開いて編集する前提
3. 役割の細分化の続き — 既存の部屋にも担当を足していく（上限10人に上げてある）

## 注意（設計上の線引き）

- 反復ドリルの問題は LLM に量産させない。型（出題条件）だけ作らせ、数値と検算は決定的なコードで
- 氏名との対応表は教材フォルダに置かない。置いた時点で「個人情報を入れない」前提が崩れる
