/**
 * 担任（あなた）の色。
 *
 * 以前は #7EACEA（水色）だったが、秘書室などが使う青灰色（#94A3B8 ほか）と
 * ほとんど見分けがつかなかった。実測すると通常視で 1.10、P型色覚では 1.02 で、
 * **事実上おなじ色**。職員室でただ一人の人間が、他に埋もれていた。
 *
 * 担当41人の色は明度50〜85に固まっているので、色相をずらすだけでは足りない
 * （黄緑や緑は、明るさが同じせいで橙や既存の緑と混ざる）。
 * 空いているのは**暗い側の明度帯**で、そこに置いた金茶がすべての検査を通った:
 *
 *   白いカードの上 4.92（P型 3.65 / D型 3.32）
 *   暗い背景の上   3.99（P型 5.46 / D型 5.98）
 *   黄と区別       3.21（P型 2.79 / D型 2.67）
 *   橙と区別       2.18（P型 2.35 / D型 2.38）
 *
 * 色を変えると3Dの担任の体もこの色になる。形は変えられない（1つのモデルを
 * 色だけ変えて並べているため）ので、3Dで担任を見分ける手がかりはこの色になる。
 */
export const USER_COLOR = '#A16207';

// Helper for alpha versions if needed in components
export const USER_COLOR_LIGHT = `${USER_COLOR}1A`; // 10%
export const USER_COLOR_SOFT = `${USER_COLOR}33`; // 20%
export const USER_COLOR_MEDIUM = `${USER_COLOR}80`; // 50%
