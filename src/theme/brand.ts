/**
 * 担任（あなた）の色。カーマイン（担任の指定、2026-08-25）。
 *
 * 以前は #7EACEA（水色）で、秘書室などの青灰色と通常視で 1.10、P型色覚では 1.02。
 * 事実上おなじ色で、職員室でただ一人の人間が他に埋もれていた。
 *
 * 936色を総当たりで検査して分かったのは、**35色すべてと区別できる色は存在しない**こと。
 * 最良の色でも、どこかの1色とは比 1.10 まで近づく。担当が35色も使っている時点で、
 * 色だけで担任を見分けさせる設計は成り立たない。
 *
 * したがってこの色に求めるのは「必ず見えること」までとし、
 * **見分けは形（猫耳・しっぽ・2頭身・大きさ）が担う**。
 * 色に頼らない手がかりを別に持たせるのは、色覚特性への対処としても定石。
 *
 *   暗い背景の上   3.70（P型 7.71 / D型 9.11）
 *   白いカードの上 5.32（P型 2.58 / D型 2.18）
 *
 * 弱点: **白いカードの上は、色覚特性のある方には目安の3.0に届かない**（2.18〜2.58）。
 * 少し暗くすれば上がるが、その分だけ暗い背景側が落ちる（#AB0028 で 2.58 になり不可）。
 * 両方を満たすカーマインは無い。職員室の主な画面が暗い背景であること、
 * 白い場所では色が面で出る（細い線ではない）ことから、この配分を選んでいる。
 *
 * この色は3Dの担任の体にも使われる。3Dは1つのモデルを色だけ変えて並べているので
 * 形を変えられず、3Dでの手がかりは色と、頭上に出る似顔絵になる。
 */
export const USER_COLOR = '#D7003A';

// Helper for alpha versions if needed in components
export const USER_COLOR_LIGHT = `${USER_COLOR}1A`; // 10%
export const USER_COLOR_SOFT = `${USER_COLOR}33`; // 20%
export const USER_COLOR_MEDIUM = `${USER_COLOR}80`; // 50%
